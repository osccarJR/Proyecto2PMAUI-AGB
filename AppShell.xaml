<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:InterfazTicketsApp.Views"
       x:Class="InterfazTicketsApp.AppShell">

    <TabBar>
        <ShellContent Title="Buscar" Icon="buscar.jpg" ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Detalles" Icon="detalles.jpg" ContentTemplate="{DataTemplate local:EventDetailPage}" />
        <ShellContent Title="Compra" Icon="compra.jpg" ContentTemplate="{DataTemplate local:PurchasePage}" />
        <ShellContent Title="Perfil" Icon="perfil.jpg" ContentTemplate="{DataTemplate local:UserProfilePage}" />
        <ShellContent Title="Comentarios" Icon="comentario.jpg" ContentTemplate="{DataTemplate local:CommentsPage}" />
    </TabBar>
</Shell>
